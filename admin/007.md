# El 007.html contiene:
- Un sistema CRUDE-ish: Crear, Leer, Actualizar título, Borrar, Buscar, Filtrar, Exportar, Selección múltiple, LocalStorage).

## Dividido en HTML, CSS y JS:
### HTML:
- header con directorio actual que muestra el directorio con sus tracks
-buscador con filtros
-reproductor basico
-formulario CREATE
-Acciones adicionales como ELIMINAR, EXPORTAR
-tabla 

const emptyTracks = () => ({mc1:[],mc2:[],mc3:[],mc4:[]});
let tracksByDirectory = {};
let nextIdByDirectory = {};

const computeNextIds = obj => { ... };
const saveLS = () => { ... };
const loadLS = () => { ... };

Creacion del objeto track:
tracksByDirectory[currentDirectory].push({
  id: nextIdByDirectory[currentDirectory]++,
  title,
  file: file.name,
  artist: artistBase || '',
  tags: tagsArray
});
